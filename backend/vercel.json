{
  "version": 2,
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["assets/**/*"],
        "maxDuration": 300
      }
    }
  ],
  "routes": [{ "src": "/api/(.*)", "dest": "server.js" }]
}
